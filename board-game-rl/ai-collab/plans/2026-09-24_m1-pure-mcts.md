# M1 計畫書：純 MCTS（井字遊戲）

> 建立：2026-09-24｜狀態：**進行中**（2026-09-24 本人確認；當天目標「能在 Gradio 跟 MCTS 下棋」已達成，S5 因此提前到 S3 之前）
> **下次從 S3 接續**（接手步驟見 [`../handover.md`](../handover.md)「下次開工」）
> 為什麼做、被否決的選項、設計取捨的完整理由：[`../notes/2026-09-24-review-and-m1-decisions.md`](../notes/2026-09-24-review-and-m1-decisions.md)
> worktree：`ml-workshop/.claude/worktrees/board-game-rl-review-4ded4f`（分支 `claude/board-game-rl-review-4ded4f`）

## 目標

1. 實作**純 MCTS**（UCT ＋ 隨機推演），在井字遊戲上做對。
2. 量出「**模擬次數 → 棋力**」曲線，找到先後手都不輸給 Alpha-Beta 的最小模擬次數 $N^*$。
3. 把原理、討論、取捨、實驗結論**寫成文件**（本專案是做中學：解釋是交付物，不是附贈品）。

**不做**（M1 之後一次一個當延伸實驗）：重用上一步的樹、「一步必勝直接下」捷徑、推演加 heuristic、對稱合併節點、平行化、調 $c$。

## 設計決策（細節與否決理由見 notes §4）

| # | 決策 | 推薦 |
|---|---|---|
| D1 | 放哪 | `agents/mcts_agent.py` ＋ `games/base.py`（`GameRules` Protocol）＋ `games/tic_tac_toe/rules.py` |
| D2 | 盤面 | 不可變 `tuple` ＋ 輪到誰 |
| D3 | 分數視角 | 節點記「走進來那一方」的得分，贏 1／平 0.5／輸 0 |
| D4 | 選步 | 拜訪次數最多的子節點 |
| D5 | $c$ | 固定 $\sqrt{2}$ |
| D6 | 強化手段 | 都不加 |
| D7 | 隨機性 | 每個 agent 自帶 `random.Random(seed)` |

## 檔案所有權

| 動作 | 檔案 |
|---|---|
| 新增 | `src/board_game_rl/games/base.py`、`src/board_game_rl/games/tic_tac_toe/rules.py`、`src/board_game_rl/agents/mcts_agent.py` |
| 新增 | `tests/games/tic_tac_toe/test_rules.py`、`tests/agents/test_mcts_agent.py` |
| 新增 | `scripts/eval_mcts.py`（掃模擬次數，輸出 JSON ＋ Markdown 報告）|
| 小改 | `src/board_game_rl/api/inference.py`（加 MCTS 分支）、`src/board_game_rl/ui/gradio_app.py`（加一個分頁）、`tests/api/test_inference.py` |
| 不動 | `engine.py`、`alphabeta_agent.py`、`q_learning_agent.py`、`dqn_agent.py`（外科式改動；勝負判定重複的技術債用差分測試管住）|

## 步驟與 done 條件

- [x] **S0 基線與小問題**（2026-09-24 完成）
  - `uv sync --locked` 成功；`pytest` 40 passed（原 28 ＋ 新 Q-Learning 12）
  - 補 `roadmap.md`、`notes/`、更新 `commands.txt`／`handover.md`／`rules.md`
- [x] **S1 規則介面**：`GameRules` Protocol ＋ 井字遊戲實作（2026-09-24）
  - `test_rules.py` 11 passed；差分測試 1,000 盤（seed `20260924`）與 `TicTacToeEngine` 逐步一致
  - 突變測試：拿掉一條勝利線 → 差分測試失敗（確認它抓得到錯）
- [x] **S2 MCTS agent**（2026-09-24）
  - Backpropagation 原定由本人親手寫，本人改請 agent 代寫（道理記在 notes §4 的 D3）
  - `test_mcts_agent.py` 17 passed；視角寫反的突變版 10 個戰術／對局測試全失敗；全套 69 passed
  - 速度：空盤面每秒約 67,000 次模擬（1,000 次 14.8 ms）
- [ ] **S3 模擬次數掃描**：`scripts/eval_mcts.py`
  - 設定：模擬次數 10／25／50／100／200／400／800／1,600；對手 Alpha-Beta 與 Random；MCTS 當 X 與當 O；每格 100 場；seed 落盤
  - 找到 $N^*$ 後在 $N^*$ 加跑每邊 500 場確認
  - 開跑前照 repo 級 `AGENTS.md §10.3` 看 `.agent-heavy-job`：是 `free` 才跑，跑前改 `busy`，跑完改回 `free`
  - done：報告 `reports/mcts_experiment_report_<日期>.md` ＋ 原始數據 JSON；勝／和／敗率、每步耗時、$N^*$；
    零敗要附**三分之一法則**的 95% 上界（$n$ 場零敗 ⇒ 真實敗率上界約 $3/n$，100 場只能說 < 3%）
- [ ] **S4 看它怎麼想**：空盤面上跑 10／100／1,000／10,000 次模擬，印出根節點每個子節點的拜訪次數與平均分
  - done：數據寫進教材當「實際例子」（偏好角落還是中央？平均分是否往 0.5 收斂？）
- [x] **S5 接上 API 與 Gradio**（2026-09-24；**今天的目標達成**）
  - `inference.py` 加 MCTS 分支，模擬次數暫定 `MCTS_SIMULATIONS = 2_000`（S3 後換成 $N^*$ 加餘裕）；Gradio 加「MCTS」分頁
  - `test_inference.py` 加 MCTS（含「當後手要擋」）
  - 實際在瀏覽器下一盤（人類當 O）：MCTS 先手下中央；人類故意不擋 1-4-7 那條線，MCTS 立刻走 7 獲勝
  - 啟動設定：repo 根 `.claude/launch.json` 的 `board-game-rl-gradio`（`uv run --directory board-game-rl --locked python src/board_game_rl/ui/gradio_app.py`，埠 7860）
- [ ] **S6 文件**
  - `docs/mcts_tutorial_<日期>.md`：照 `rules.md` 教材格式（日期、Mermaid 圖解、數值推演、高中生版、專業版）
  - `notes/`：把 D1 至 D7 的「待確認」改成「定案」，補實作中遇到的取捨與坑
  - `dev_log.md`、`roadmap.md`、`handover.md`、`project_guide.md`、`README.md`、`docs/learning_path.md`
- [ ] **S7 驗收**
  - `uv run pytest` 全過；repo 根 `pre-commit run --all-files` 通過
  - 教材的 Mermaid 用 Chrome 實際渲染確認；外部連結實測可連

## 風險

| 風險 | 怎麼發現／處理 |
|---|---|
| D3 視角寫反 ⇒ MCTS 幫對手下 | S2 的「必勝要拿、必輸要擋」測試 |
| 純 Python 太慢，1,600 次模擬的掃描跑太久 | S2 先量每秒模擬次數再定每格場數；單 process，不搶資源 |
| 1,600 次還是會輸給 Alpha-Beta | 不算失敗：做中學的產出是**解釋為什麼**，把輸掉的棋局抓出來分析寫進報告 |
| Alpha-Beta 是確定性對手，對局多樣性有限 | 同時報告對 Random 的數字；報告中明講這個限制 |
| 零敗被過度解讀 | 一律附三分之一法則的上界 |

## 待本人確認

1. ~~D1 至 D7 照推薦走？~~ 2026-09-24 確認
2. `pytest` 列入 dev 相依：`uv add --dev pytest`（本人執行，或明確授權由 agent 執行）——**還沒回覆**
3. ~~S5 放在 M1 範圍內？~~ 2026-09-24 確認，而且是今天的目標
