# Onboarding Commands (上手指南)
# Last Updated: 2026-09-24

---

## For AI Agent (給 AI 助手的初始 Prompt)

### 第一步：了解規範與架構
0. 去看 ai-collab/roadmap.md，了解現況與下一步（第一站）
1. 去看 ai-collab/rules.md，了解核心合作開發模式
2. 去看 ai-collab/project_guide.md，了解專案架構
3. 去看 ai-collab/dev_log.md，了解最新開發進度
4. 去看 ai-collab/handover.md，了解已完成功能與環境確認清單
4b. 去看 ai-collab/notes/，了解過去討論與決策的來龍去脈

### 第二步：建立心智模型
5. 去看 pyproject.toml，了解 Python 依賴
6. 去看 src/board_game_rl/ 目錄結構，了解程式碼架構
7. 去看 docs/ 了解已有的演算法教材
8. 去看 README.md 了解對外文件

### 第三步：環境檢查
9. 確認 Python 3.13 + uv 已安裝
10. 執行 `uv sync --locked` 確認依賴正確（依 lockfile，不重新解析版本）
11. 執行 `uv run pytest tests/ -v` 確認測試全過（2026-09-24：40 passed）
    - `pytest` 列入 dev 相依之前，改用 `uv run --with pytest pytest tests/ -v`
12. 確認 models/q_table.json、models/alphabeta_cache.json、models/dqn_model.pth 存在

### 第四步：確認最新狀態
13. 去看 git branch name 以及 git log
14. 綜合以上資訊，向 Developer 回報並詢問下一步

### 當前重點任務
- 正本在 `ai-collab/roadmap.md` 的「下一步」，本檔不重複維護

---

## For Human Developer

1. 閱讀 `ai-collab/rules.md`
2. 閱讀 `ai-collab/project_guide.md`
3. 閱讀 `README.md`
4. 執行：`cd board-game-rl/ && uv sync`

---

## Common Workflows

```bash
# 安裝依賴
cd board-game-rl/
uv sync

# Web UI 對弈 (localhost:7860)
uv run python src/board_game_rl/ui/gradio_app.py

# 終端對戰
uv run python scripts/play.py

# 訓練 Q-Learning (150K 場，自動產出報告)
uv run python scripts/train_q_learning.py

# 預計算 Alpha-Beta 快取
uv run python scripts/precompute_alphabeta.py

# 測試（pytest 列入 dev 相依之前：uv run --with pytest pytest tests/ -v）
uv run pytest tests/ -v

# Docker 啟動 (含 FastAPI + Gradio)
docker compose up -d
```

---

## Commit 規範提醒
- 使用 Conventional Commits，必須包含 scope
- 範例：`feat(rl): add DQN agent implementation`
- 禁止 force push


claude --dangerously-skip-permissions